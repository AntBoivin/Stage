
\newif\ifwhole

\wholetrue
% Ajouter \wholetrue si on compile seulement ce fichier

\ifwhole
 \documentclass[a4page,10pt]{report}
     \input{entete}
 \begin{document}
\input{enonce}

\fi

%\begin{Thm} \label{famille_parametre}
%Soit $S$ un espace analytique réduit. Pour chaque famille analytique $(X_s)_{s \in S}$ de cycles compacts de dimension $n$ de $Z$ paramétrée par S, le sous-ensemble $Y= \{(s,z) \in S \times Z \mid z \in  |X_s| \}$ de $S \times Z$ est analytique fermé. La restriction à $Y$ de la projection naturelle sur $S$ est propre, surjective et à fibres de dimension $n$. Si $S$ est irréductible, pour chaque composante irréductible $Y_i$ de $Y$, il existe un entier $n_i\geq 0$, tel que les multiplicités des composantes irréductibles de $|X_s|$ contenues dans $Y_i$ soient génériquement sur $S$ égales à $n_i$. \\
%Réciproquement, si $S$ est normal, la donnéé d'un nombre fini de sous-ensembles analytiques $ Y_i$ de $S \times Z$ affectés d'entiers $n_i > 0$, tels que les projections $Y_i \to S$ soient propres, surjectives et à fibres de dimension $n$, définit une famille analytique $(X_s)_{s \in S}$ de cycles de dimension $n$ de $Z$ paramétrés par $S$.
%
%\end{Thm}

\section{}
%\begin{Thm}[du rang constant]
%Soient $M \subset \C^n$ et $N \subset \C^p$ deux ouverts et $f : M \to N$ une application holomorphe de rang constant égal à $r$ (i.e pour tout $x \in M,rk(d_x f)=r$ ). Alors, pour tout $x \in M$, il existe des voisinages ouverts de $x$ dans $M$ et $W$ de $f(x)$ dans $N$ et des applications biholomorphes $\varphi : V \to V_1$ et $\psi : W \to W_1$ sur des voisinages ouverts $V_1$ et $W_1$ de l'origine respectivement dans $\C^n$ et $\C^p$ qui vérifient les conditions :
%\begin{enumerate}
	%\item $\varphi(V)=V_1=U \times U_1$ et $\psi(W)=W_1=U \times U_2$, $U \in\C^r$, $U_1 \subset \C^{n-r}$, $U_2 \subset \C^{p-r}$
	%\item $\forall (z_1,z_2) \in U \times U_1,\psi \circ f \circ \varphi^{-1}(z_1,z_2)=(z_1,0)$
%\end{enumerate}
%
%\end{Thm}
%\begin{proof}
%\cite{\Dieu}
%\end{proof}
%Une autre façon de voir ce théorème est qu'en tout point de $M$, il existe un voisinage $V$ de $x$ tel que $f(V)$ soit une variété de dimension $r$ (qui est égal à $\varphi^{-1}(U \times \{0\})$).
%\begin{Cor}
%Soit $f : M\to N$ une application holomorphe entre deux variétés complexes de rang constant. Alors, toute fibre de $f$ est une sous-variété complexe de $M$ de dimension égale à $dim(M)-rk(df)$.
%
%\end{Cor}
%\begin{proof}
%Notons $r:=rk(d_xf)$. \\
%Soit $y=f(x) \in N$. Comme $N$ est une variété alors il existe une carte de $N$, $(V,\varphi : V \to \C^{\dim(N)})$, autour de $y$. De la même façon, on a une carte $(U,\psi :U \to \C^{\dim(M)})$ autour de $x$ tel que $f(U) \subset V$. On applique le théorème du rang constant à $f_1=\psi \circ f \circ \varphi^{-1}:\varphi(U)\to \psi(V)$. Il existe donc un ouvert $U' \subset \psi(U)$ et $V' \subset \psi(V)$ et des isomorphismes $\lambda : U' \simeq U'' \times U_1$ et $\mu : V' \simeq U'' \times U_2$ tels que :
	%\[\forall (z_1,z_2) \in U'' \times U_1, f_2(z_1,z_2):=\mu \circ f \circ \lambda^{-1}(z_1,z_2)=(z_1,0).
	%\]
	%\\
%Ainsi, $f_2^{-1}(0)$ et $\lambda\circ \varphi$ nous donne une carte de $f^{-1}(y)$ autour de $x$, ce qui montre que $f^{-1}(y)$ est une variété.
%\end{proof}
%\begin{Cor}
%Soit $f : M \to N$ une application holomorphe de rang constant $q<\dim(N)$. Alors $f(M)$ est d'intérieur vide dans $N$. 
%
%\end{Cor}
%
%\begin{proof}
%Soit $x \in X$. Par le théorème du rang constant, il existe un voisinage $V$ de $x$ tel que $f(V)$ est une variété de dimension $q$. Par un argument de dimension, on déduit que $f(V)$ est d'intérieur vide. Ainsi, l'image de tout voisinage compact de $x$ dans $V$ est d'intérieur vide dans $N$. Comme $M$ peut être recouvert par de tels voisinages compacts alors $f(M)$ est aussi d'intérieur vide car $M$ est localement compact (propriété de Baire).
%
%\end{proof}
%Supposons $M$ et $N$ connexes (et donc irréductible) pour simplifier les explications : \\
%Si $k$ est le rang maximal d'une application $f$ alors les points où $f$ sont de rang strictement inférieur sont des espaces analytiques d'intérieur vide (comme zéros des mineurs qui sont des fonctions holomorphes). Ainsi, le complémentaire de l'union (finie !) de ces espaces analytiques est un ouvert de Zariski et quitte à l'intersecter avec son lieu régulier, on peut supposer que c'est une variété. \\
%Ainsi, si on suppose $f$ surjective alors $k=\dim(N)$. Sur notre ouvert, les fibres sont des variétés complexes de dimension $\dim(M)-\dim(N)$ ou autrement dit, $f$ est une submersion. Cela nous permet de montrer le résultat suivant : 
%
%\begin{Thm} \label{submersion}
%Soit $f :X \to Y$ une application surjective holomorphe entre un espace analytique réduit irréductible et une variété connexe. Alors, il existe un ouvert de Zariski $U$ de $X$ (qui est lisse) telle que $f_{|U}$ est une submersion.
%
%\end{Thm}

%\section{}
%
%\begin{Def}
%Soit $f : X \to Y$ une application entre deux espaces analytiques réduits. Un $n$-cycle $Z$ de $X$ est dit $f$-relatif s'il existe $y \in N$ tel que l'on ait l'inclusion $|Z| \subset f^{-1}(y)$.\\
 %On notera $\Cscr_n(f)$ les $n$-cycles $f$-relatifs de $M$
%\end{Def}
%
%\begin{Thm}
%Soit $f : X \to Y$ une application entre deux espaces analytiques réduits. Alors, l'ensemble $\Cscr_n(f)$ est un sous-espace analytique de $\Cscr_n(X)$
%\end{Thm}
%
%\begin{proof}
%Voir Chap 4 8.2.2
%
%\end{proof}
%
%\begin{Prop} \label{Graphe_analyt}
%Soit $f : X \to Y$ une application holomorphe entre deux espaces analytiques réduits. Alors, $\{(y,Z) \mid |Z| \subset f^{-1}(y)\}$ est un sous-espace analytique fermé dans $N \times \Cscr_N(M)$
%
%\end{Prop}

%\subsection{Morphisme}
%
%Dans ce paragraphe, on va étendre la définition de submersion au cas général des espaces analytiques :  
%
%\begin{Def}
%Soit $A$ un anneau commutatif. Un $A$-module $M$ est dit $A$-plat si pour toute morphisme injectif $N_1 \to N_2$ de $A$-modules, le morphisme induit $N_1 \times_A M \to N_2 \times_A M$ est injectif. \\
%Autrement dit, $M$ est $A$-plat si le foncteur $\-- \otimes M$ est exact.
%\end{Def}
%
%\begin{Def}
%Une application holomorphe $f : X \to Y$ entre deux espaces analytiques réduits est dite plate si, pour tout $x \in X$, $\Ocal_{X,x}$ est un $\Ocal_{Y,f(x)}$-module plat (où l'action de $\Ocal_{Y,f(x)}$ sur $\Ocal_{X,x}$ est donnée par le morphisme induit dans les fibres $f_x^\sharp : \Ocal_{Y,f(x)} \to \Ocal_{X,x}$).
%
%\end{Def}
%
%\begin{Def}
%Un application holomorphe $f :X \to Y$ est appelée morphisme lisse si $f$ est plate et si le faisceau $coker(df : f^* \Omega^1_Y \to \Omega^1_X)$ est localement libre de dimension $\dim(X)-\dim(Y)$ (où on définit localement $df$ sur les espaces modèles comme la différentielle usuelle)
%\end{Def}
%
%Pour mieux comprendre cette définition, regardons le cas où $X$ et $Y$ sont des variétés : \\
%La deuxième condition nous dit que $f$ est une submersion. Mais, de plus, on peut montrer que si $f$ est une submersion alors elle est plate. La notion de morphisme lisse étend donc celle de submersion du cas lisse. De plus, on a le résultat, classique pour le cas lisse, suivant :
%
%\begin{Prop}
%Soit $f : X \to Y$ un morphisme surjectif lisse. Alors les fibres de $f$ sont lisses et de dimension $\dim(Y)-\dim(X)$.
%
%\end{Prop}

\section{}

%\begin{Thm}[Oka] \label{non_normal}
%L'ensemble $N(X)$ des points non-normaux d'un espace analytique réduit est un sous-espace analytique de $X$ qui est contenu dans le lieu singulier $S(X)$.
%\end{Thm}
%
%\begin{proof}
%Voir \cite{\scv} section 6 paragraphe 5
%\end{proof}


%\begin{proof}
%Soit $Y''$ l'ensemble des points $y \in Y$ normaux dont la fibre est de dimension maximale $n$. De la même façon que dans la preuve de \ref{biholo}, l'ensemble des points dont la fibre est de dimension maximale est un ouvert de Zariski car $f$ est surjective et propre. De plus, par la proposition \ref{non_normal}, l'ensemble $N(X)$ des points normaux est un ouvert de Zariski. Ainsi, $N''$ est un ouvert de Zariski de $Y$. 
%Alors, par le théorème \ref{famille_parametre} avec le graphe de $\Gamma$ (ou du moins, sa décomposition en irréductibles), on obtient l'unique application holomorphe $\varphi'' : Y'' \to \Cscr_n(X)$ telle que $(\varphi(y))_{y \in Y}$ est une famille analytique dont les cycles coïncident génériquement avec les fibres de $f$. \\
%Soit $\widetilde{Y}$ l'adhérence du graphe de $\varphi$ dans $Y \times \Cscr_n(X)$. Pour obtenir une application méromorphe, il nous faut montrer que c'est un espace analytique  \\
%Soit $\Gamma:=\{(y,Z) \mid |Z| \subset f^{-1}(y)\}$. C'est un espace analytique par \ref{Graph_analyt}. De plus, $\Gamma$ contient $\widetilde{N}$. Il existe une composante irréductible $\Gamma_0$ de $\Gamma$ qui contient $\widetilde{N}$ et ce dernier contient un ouvert non vide de $\Gamma_0$. \\
%En effet, si on prend $(y_0,Z_0) \in \widetilde{N}$ suffisamment proche  et en considérant une famille finie  (car $f^{-1}(y_0)$ est compact) d'écailles adaptée à $f^{-1}(y_0)$ dont les centres recouvrent $f^{-1}(y_0)$, on voit que tout $(y,Z)$ ``suffisamment proche" vérifie $X=f^{-1}(y)$ grâce à \ref{famille_cont}. \\
%L'ensemble $Y''$ est Zariski-ouvert dans $Y$ et donc l'image réciproque du complémentaire de $Y''$ (qui est donc de dimension inférieure à $N$) est analytique fermée mais différent de $\Gamma_0$ (pour des questions de dimension) et est donc d'intérieur vide dans $\Gamma_0$. Autrement dit, le graphe de $\varphi''$ est dense dans $\Gamma_0$ i.e. $\widetilde{Y}=\Gamma_0$. On en déduit que $\widetilde{Y}$ est analytique. \\
%Pour finir de montrer que c'est bien une application méromorphe, il faut montrer que $\tau :\widetilde{Y} \to Y$ est une modification propre. \\
%Par définition, $\tau$ induit un isomorphisme entre le graphe de $\varphi''$ et $Y''$. Il ne reste plus qu'à montrer que $\tau$ est propre. \\
%Soit $K$ un compact de $Y$ et considérons l'ensemble $\pi_2(\tau^{-1}(K))$ où $\pi_2 : \widetilde{Y} \to \Cscr_n(X)$ \\ 
%On remarque que tous les support des cycles de $\pi_2(\tau^{-1}(K))$ sont inclus dans le compact $f^{-1}(K)$ (car $f$ est compact). L'application $s \in K \mapsto \frac{1}{k!}\int_{Z_s} \omega^k$ est une application continue (voir démonstration \ref{compact} pour les détails) et est bornée car $K$ est compact. On en déduit par le théorème \ref{compact} que $\pi_2(\tau^{-1}(K))$ est relativement compact et donc $\tau^{-1}(K)$ est inclus dans le compact $K \times \pi_2(\tau^{-1}(K))$ et est donc compact. Cela montre que $\tau$ est une application propre et donc que l'on a bien défini une application méromorphe $Y \dashrightarrow \Cscr_n(X)$
%\end{proof}





\ifwhole
 \end{document}
\fi